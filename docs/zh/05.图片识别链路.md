# 05.图片识别链路

图片识别负责把前端上传的图片交给 OpenAI 兼容 Vision API 分析。
本篇讲图片从 `/vision` 页面上传，到后端调用视觉模型并返回结果的链路。

## 1.完整链路

```text
/vision 页面
  -> ImageRecognition.tsx
  -> imageApi.recognize
  -> POST /api/v1/image/recognize
  -> Router
  -> controller
  -> service
  -> 图片转 base64 data URL
  -> OpenAI 兼容 Vision API
  -> controller 返回 class_name
  -> frontend 展示识别结果
```

对应到项目文件：

```text
GoNexus/frontend/src/App.tsx
  -> GoNexus/frontend/src/pages/VisionPage.tsx
  -> GoNexus/frontend/src/components/chat/ImageRecognition.tsx
  -> GoNexus/frontend/src/api/image.ts
  -> GoNexus/router/Image.go
  -> GoNexus/controller/image/image.go
  -> GoNexus/service/image/image.go
  -> GoNexus/config/config.go
```

## 2.Frontend：进入图片识别页面

文件：`GoNexus/frontend/src/App.tsx`

```tsx
// /vision 路由对应图片识别页面
<Route path="/vision" element={
  <ProtectedRoute>
    <VisionPage />
  </ProtectedRoute>
} />
```

文件：`GoNexus/frontend/src/pages/VisionPage.tsx`

```tsx
export function VisionPage() {
  return (
    <main className="bg-white border-4 border-black p-8 shadow-brutal min-h-[60vh] flex flex-col">
      {/* 图片识别的主要组件 */}
      <ImageRecognition />
    </main>
  )
}
```

## 3.POST：前端选择图片并调用接口

文件：`GoNexus/frontend/src/components/chat/ImageRecognition.tsx`

```tsx
const handleStartAnalysis = async () => {
  if (!selectedFile || status !== 'idle') return

  // 进入分析中状态
  setStatus('analyzing')
  try {
    // 调用后端图片识别接口
    const res = await imageApi.recognize(selectedFile)

    if (res.data?.status_code === 1000) {
      // 保存后端返回的识别结果
      setBackendResult(res.data.class_name)
      setStatus('done')
    } else {
      setStatus('error')
    }
  } catch (err) {
    setStatus('error')
  }
}
```

文件：`GoNexus/frontend/src/api/image.ts`

```ts
recognize: (file: File) => {
  const formData = new FormData()

  // 后端 controller 通过 FormFile("image") 读取
  formData.append('image', file)

  // 上传到 /api/v1/image/recognize
  return client.post('/image/recognize', formData, {
    headers: {
      'Content-Type': 'multipart/form-data',
    },
  })
}
```

## 4.Router：匹配图片识别接口

文件：`GoNexus/router/Image.go`

```go
func ImageRouter(r *gin.RouterGroup) {
	// 图片识别接口
	r.POST("/recognize", image.RecognizeImage)
}
```

## 5.Controller：读取图片并调用 service

文件：`GoNexus/controller/image/image.go`

```go
type RecognizeImageResponse struct {
	// AI 视觉模型返回的图片描述或识别结果
	ClassName string `json:"class_name,omitempty"`
	controller.Response
}
```

```go
func RecognizeImage(c *gin.Context) {
	res := new(RecognizeImageResponse)

	// 从 multipart 表单 image 字段读取图片
	file, err := c.FormFile("image")
	if err != nil {
		log.Println("FormFile fail ", err)
		c.JSON(http.StatusOK, res.CodeOf(code.CodeInvalidParams))
		return
	}

	// 交给 service 调用视觉模型分析
	className, err := image.RecognizeImage(file)
	if err != nil {
		log.Println("RecognizeImage fail ", err)
		c.JSON(http.StatusOK, res.CodeOf(code.CodeServerBusy))
		return
	}

	// 返回识别结果
	res.Success()
	res.ClassName = className
	c.JSON(http.StatusOK, res)
}
```

## 6.Service：读取图片内容

文件：`GoNexus/service/image/image.go`

```go
func RecognizeImage(file *multipart.FileHeader) (string, error) {
	log.Printf("[Backend] Starting AI Vision analysis for: %s", file.Filename)

	// 打开上传图片
	src, err := file.Open()
	if err != nil {
		return "", fmt.Errorf("failed to open image: %v", err)
	}
	defer src.Close()

	// 读取图片二进制内容
	imgData, err := io.ReadAll(src)
	if err != nil {
		return "", fmt.Errorf("failed to read image data: %v", err)
	}
```

## 7.Service：转换成 base64 data URL

文件：`GoNexus/service/image/image.go`

```go
ext := strings.ToLower(filepath.Ext(file.Filename))
mimeType := "image/jpeg"

// 根据后缀设置图片 MIME 类型
if ext == ".png" {
	mimeType = "image/png"
} else if ext == ".webp" {
	mimeType = "image/webp"
}

// 把图片转成 OpenAI 兼容接口可接收的 data URL
dataURL := fmt.Sprintf("data:%s;base64,%s", mimeType, base64.StdEncoding.EncodeToString(imgData))
```

## 8.Service：读取 Vision API 配置

文件：`GoNexus/service/image/image.go`

```go
conf := config.GetConfig()

// Vision API 密钥
apiKey := conf.GetVisionAPIKey()

// Vision API base url
baseURL := conf.GetVisionBaseURL()

// Vision 模型 ID
modelID := conf.GetVisionModelID()

if apiKey == "" || baseURL == "" || modelID == "" {
	return "", fmt.Errorf("AI API key, base URL, or model ID is missing in config")
}
```

## 9.Service：构造 OpenAI 兼容请求体

文件：`GoNexus/service/image/image.go`

```go
reqBody := OpenAICompatibleVisionRequest{
	// 使用配置里的视觉模型
	Model: modelID,
	Messages: []Message{
		{
			Role: "user",
			Content: []Content{
				{
					// 文本提示词
					Type: "text",
					Text: "You are the GoNexus Intelligence Engine. Please analyze this image and provide a concise, professional description of the main objects, colors, and key features. Return the result in pure text.",
				},
				{
					// 图片内容
					Type: "image_url",
					ImageURL: &ImageURL{
						URL: dataURL,
					},
				},
			},
		},
	},
}
```

## 10.Service：请求 Vision API

文件：`GoNexus/service/image/image.go`

```go
// OpenAI 兼容接口地址
apiEndpoint := strings.TrimRight(baseURL, "/") + "/chat/completions"

// 创建 HTTP POST 请求
req, err := http.NewRequest("POST", apiEndpoint, bytes.NewBuffer(jsonPayload))
if err != nil {
	return "", fmt.Errorf("failed to create HTTP request: %v", err)
}

// 设置 JSON 和 Bearer token
req.Header.Set("Content-Type", "application/json")
req.Header.Set("Authorization", "Bearer "+apiKey)

// 调用视觉模型接口
resp, err := http.DefaultClient.Do(req)
if err != nil {
	return "", fmt.Errorf("failed to call AI Vision API: %v", err)
}
defer resp.Body.Close()
```

## 11.Service：解析模型返回

文件：`GoNexus/service/image/image.go`

```go
// 读取模型响应体
respBody, err := io.ReadAll(resp.Body)
if err != nil {
	return "", fmt.Errorf("failed to read API response: %v", err)
}

var aiResp AIResponse

// 解析 OpenAI 兼容响应 JSON
if err := json.Unmarshal(respBody, &aiResp); err != nil {
	log.Printf("Raw API Response: %s", string(respBody))
	return "", fmt.Errorf("failed to parse AI response JSON: %v", err)
}

// 模型接口返回错误
if aiResp.Error != nil {
	return "", fmt.Errorf("AI API Error: %s", aiResp.Error.Message)
}

// 返回第一条回答内容
if len(aiResp.Choices) > 0 {
	return aiResp.Choices[0].Message.Content, nil
}

return "No result generated by AI Vision model.", nil
```

## 12.Frontend：展示识别结果

文件：`GoNexus/frontend/src/components/chat/ImageRecognition.tsx`

```tsx
{status === 'done' && backendResult && (
  <motion.div className="space-y-4">
    <div className="bg-white border-4 border-black p-4 shadow-brutal">
      {/* 展示后端返回的图片分析文本 */}
      <p className="font-black text-left leading-tight text-lg">{backendResult}</p>
    </div>
  </motion.div>
)}
```
